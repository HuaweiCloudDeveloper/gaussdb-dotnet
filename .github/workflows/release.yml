name: Release

on:
  workflow_dispatch: 
  push:
    tags:
      - 'v*.*.*' # e.g., v1.0.0

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET SDK
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: |
          8.0.x
          9.0.x          

    - name: Build project
      run: dotnet build -c Release

    - name: Pack NuGet package
      if: startsWith(github.ref, 'refs/tags/')
      run: |
        dotnet pack ./src/GaussDB/GaussDB.csproj -c Release -o ./artifacts/nupkgs -p VersionPrefix='${GITHUB_REF_NAME#v}'

    - name: Pack Preview NuGet package
      if: github.event_name == 'workflow_dispatch' || github.ref == 'refs/heads/preview'
      run: |
        dotnet pack ./src/GaussDB/GaussDB.csproj -c Release -o ./artifacts/nupkgs -p VersionSuffix='$(preview-[System.DateTime]::UtcNow.ToString("yyyyMMdd"))'

    - name: Push to nuget.org
      run: |
        dotnet nuget push "./nupkgs/*.nupkg" -k ${{ secrets.NUGET_API_KEY }}
